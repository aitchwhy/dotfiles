meta {
  name: Update split DNS
  type: http
  seq: 8
}

patch {
  url: {{baseUrl}}/tailnet/:tailnet/dns/split-dns
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}
