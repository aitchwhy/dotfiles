# User-level npm configuration
# This file is symlinked to ~/.npmrc

# Set the global packages directory
prefix=${HOME}/dotfiles/.config/npm-global

# Cache directory
cache=${HOME}/.cache/npm

# Initialize settings for new projects
init-author-name=Hank
init-license=MIT
init-version=0.1.0

# Development settings
save=true
save-exact=false
save-prefix=^

# Security and performance
audit-level=moderate
fund=false
update-notifier=false

# Registry settings
registry=https://registry.npmjs.org/

# Log level
loglevel=warn

# Editor preference
editor=nvim

# Shell
shell=/bin/zsh