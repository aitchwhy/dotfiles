# Nix build outputs
result

# Generated files
repomix-output.xml
state.yml
.zcompdump
.zcompdump-*

# macOS
.DS_Store
**/.DS_Store

# Package manager cache
.config/configstore/

# Auto-backups
*.old.json
*-backup.txt

# GitHub Copilot state
.config/github-copilot/versions.json
.config/github-copilot/apps.json

# API specs (fetch dynamically)
lib/clients/api/specs/

# Claude Code local settings (user-specific, not config)
.claude/settings.local.json

# Evolution system runtime metrics (machine-specific)
.claude-metrics/

# Secrets (never commit)
.env
.env.local
.env.*.local
.envrc
*.gpg
.dev.vars

# Security: PII and session data
config/wispr-flow/
config/httpie/sessions/
.pre-commit-config.yaml
