# Nix build outputs
result
result-*

# Generated files - Repomix
repomix-output.xml
**/repomix-output.xml
**/repomix-*.xml
state.yml
.zcompdump
.zcompdump-*

# macOS
.DS_Store
**/.DS_Store

# Package manager cache
.config/configstore/

# Lock files (PARAGON Guard 3 - use package.json for deps)
bun.lock
bun.lockb
package-lock.json
yarn.lock

# Auto-backups
*.old.json
*-backup.txt

# GitHub Copilot state
.config/github-copilot/versions.json
.config/github-copilot/apps.json

# API specs (fetch dynamically)
lib/clients/api/specs/

# Evolution system generated output
config/agents/memory/reflections/

# Claude Code local settings (user-specific, not config)
.claude/settings.local.json

# Third-party Claude Code plugins (generated, not managed)
.plugin-config/
.complexity-log.md
.pair-programming-session.md

# Evolution system runtime metrics (machine-specific)
.claude-metrics/

# TDD bypass file (temporary, never commit)
.tdd-skip

# Secrets (never commit)
.env
.env.local
.env.*.local
.envrc
*.gpg
.dev.vars

# Security: PII and session data
config/wispr-flow/
config/httpie/sessions/
.pre-commit-config.yaml

# Health domain - sensitive personal data
domains/health/snapshots/

# Quality system generated files
config/quality/generated/

# Editor backups
*.bak
*~

# macOS extras
._*
.Spotlight-V100
.Trashes
