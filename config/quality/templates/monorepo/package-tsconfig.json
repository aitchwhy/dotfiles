{
  "_comment": "SSOT package tsconfig (Dec 2025) - source-only, no compilation",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src"],
  "exclude": ["node_modules"]
}
