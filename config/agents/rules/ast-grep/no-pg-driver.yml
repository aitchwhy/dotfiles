id: paragon-no-pg-driver
language: typescript
severity: error
message: "Guard 47: 'pg' driver forbidden. Use 'postgres' (postgres.js) instead."

rule:
  any:
    - pattern: import $_ from 'pg'
    - pattern: import { $$$_ } from 'pg'
    - pattern: require('pg')
