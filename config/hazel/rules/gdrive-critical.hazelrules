# Google Drive Critical Rule
# Purpose: Remove macOS system clutter files
# Status: ACTIVE (Phase 1 - Critical Only)
# Apply to: /Users/hank/Library/CloudStorage/GoogleDrive-hank.lee.qed@gmail.com/My Drive/My Drive

## Rule: Continuous .DS_Store cleanup
## Remove macOS .DS_Store files immediately
Conditions:
- Name is ".DS_Store"
- Kind is "File"

Actions:
- Move to Trash immediately

---

# Implementation Notes:
# - Import this rule into Hazel app manually: Hazel → My Drive → Import Rules
# - IMPORTANT: Check "Include subfolders" when setting up the folder in Hazel
# - Zero risk: .DS_Store files serve no user purpose (macOS metadata only)
# - High value: Prevents Google Drive from syncing useless files

# Future Rules (Available in archive/gdrive-lifecycle.hazelrules):
# - Inbox flagging (3 days)
# - Active → Recent transition (30 days inactive)
# - Recent → Archive transition (90 days inactive)
# - Archive → Cold Storage transition (1 year inactive)
# - Root file organization (14 days)
