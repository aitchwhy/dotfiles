Regenerate Claude Code artifacts from TypeScript SSOT:

1. `cd ~/dotfiles/config/quality`
2. `bun install --frozen-lockfile`
3. `bun run generate`
4. Verify no errors in output
5. Report counts: skills, personas, rules, memories
